{
  "name": "tw5-intensedebate",
  "author": "bimlas",
  "scripts": {
    "build": "npm run build:html; npm run build:json",
    "build:html": "TIDDLYWIKI_PLUGIN_PATH=plugins tiddlywiki --build index",
    "build:json": "TIDDLYWIKI_PLUGIN_PATH=plugins tiddlywiki --render '$:/plugins/bimlas/intensedebate' tw5-intensedebate.json text/plain '$:/core/templates/json-tiddler'"
  },
  "dependencies": {
    "tiddlywiki": "^5.3.3"
  }
}
